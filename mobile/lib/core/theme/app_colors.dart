import 'package:flutter/material.dart';

/// tayyorr.uz veb-saytining rang palitrasi bilan bir xil.
class AppColors {
  AppColors._();

  static const bg = Color(0xFF07070C);
  static const surface = Color(0xFF0B0B12);
  static const surfaceAlt = Color(0xFF0E0E16);
  static const card = Color(0x0DFFFFFF); // white / 5%
  static const cardBorder = Color(0x1AFFFFFF); // white / 10%

  static const indigo = Color(0xFF6366F1);
  static const indigoStrong = Color(0xFF4F46E5);
  static const violet = Color(0xFF7C3AED);

  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFFA1A1AA); // zinc-400
  static const textMuted = Color(0xFF71717A); // zinc-500
  static const textFaint = Color(0xFF52525B); // zinc-600

  static const emerald = Color(0xFF34D399);
  static const amber = Color(0xFFFBBF24);
  static const red = Color(0xFFF87171);
}
